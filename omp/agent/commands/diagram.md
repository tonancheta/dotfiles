---
description: Technical diagram-as-code (Mermaid/Graphviz/PlantUML/D2) via NVIDIA-hosted DeepSeek R1
---
Use the task tool to dispatch to the "diagram" agent with this exact request, then
report its diagram source back to me verbatim in a fenced code block. If I asked for
a rendered image or a saved file, render/save the source yourself after checking the
syntax is valid: $ARGUMENTS
